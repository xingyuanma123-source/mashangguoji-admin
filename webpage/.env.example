# ============================================================
# webpage 前端环境变量示例
# ============================================================
# 重要：前端运行时走【同源相对路径】 /api/db、/api/agent、/api/ocr，
# 由 Nginx 反向代理到对应的后端进程。
# 因此 webpage 的 bundle 是【环境无关】的 —— staging / prod 的切换发生在
# Nginx 服务层（哪个站点把 /api/* 反代到哪个端口），不在前端构建期 env。
# 一般情况下本文件的变量【都不需要设置】。
# ============================================================

# 可选：覆盖 supabase 客户端基址。不设时默认同源 /api/db/supabase（推荐留空）。
# VITE_SUPABASE_PROXY_URL=

# 仅供离线脚本 scripts/import-documents/upload.ts 使用（不影响前端运行时）。
# 值为对应环境 Supabase 的 URL 与 anon key（anon 为公开级，可放心填）。
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key

# 以下变量当前源码未引用，疑似历史遗留（保留以备查，可后续清理）：
# VITE_APP_ID=
# VITE_TENCENT_MAP_KEY=
