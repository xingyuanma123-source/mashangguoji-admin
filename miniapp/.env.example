# ============================================================
# miniapp（Taro 小程序）环境变量示例
# ============================================================
# Taro 在【构建期】注入 TARO_APP_* 变量。小程序【直连 Supabase】，
# 因此 staging / prod 必须使用不同的库地址与 anon key。
#
# 按 mode 加载：
#   taro build --mode staging     → 读取 .env.staging（连 staging 库）
#   taro build --mode production  → 读取 .env.production（连 prod 库，发布用）
#
# 只放【公开级】值：Supabase URL + anon key + 小程序 appid。
# 绝不放 service_role 或任何高权限密钥（小程序 bundle 会下发到用户端）。
# ============================================================

TARO_APP_SUPABASE_URL=https://your-project.supabase.co
TARO_APP_SUPABASE_ANON_KEY=your-anon-key
TARO_APP_APP_ID=your-app-id
