# 车辆派遣管理模块 — 开发 Spec（v1）

> 给 Codex 的实现规格。格式约定：先讲 **what / why**，再讲 **how**；中文为主，技术术语 / 字段名用英文。

---

## 1. 背景与目标（Why）

马上国际是自有车队的跨境货运公司。当前客服在一张 Excel（一月一个 sheet，行=车、列=日期 1–31）里手工排车。核心痛点：**部分车辆长期没被派过，闲置/分配不均看不出来**。

本模块把这张 Excel 搬进现有全栈系统，做成**客服端的车辆派遣可视化看板（车辆派遣管理表）**，第一版要解决的就是「让闲置车浮现」+「客服按车型快速找到合适的空闲车」。

**第一版明确不做**（避免范围蔓延）：订单/运单系统、车队长派人、司机主数据、车挂/柜号总表派单记录、光头挂车管理、小程序端。这些都在文末「Out of scope」里列出，给后续留接口。

---

## 2. 业务流程现状（context）

1. 客服根据来的单子，选**合适的车型/车辆**
2. 车队长**派人接单**（此步不进本表）
3. 没人接 → 客服**找代驾**（本版只在派车记录上打一个「代驾」标记，不记是谁）

本表只承载到：**某辆车 + 某一天 → 被哪个货主/客户用了 + 由哪个客服经手**。

---

## 3. 核心数据模型（What）

### 3.1 语义澄清（重要）

Excel 格子里的 `富-黄` ＝ **货主/客户（富）- 客服（黄）**，**不是司机**。
一车一天可有多条记录（多个客户 / 多趟）。

### 3.2 数据落库原则

- **底层按真实日期存**：一条派车记录 = `vehicle + dispatch_date + customer + agent(客服)`。
- **不存在「sheet / 31 列」概念**，那只是展示层。展示时按所选月份把记录铺成日历。

### 3.3 表结构

> 复用现有车辆主数据；客服身份取自现有登录用户体系。下面只定义本模块新增 / 复用的关键字段。

#### `vehicles`（车辆主数据，复用现有表，按需补字段）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | uuid PK | |
| plate_no | text | 车牌，如 `桂FB0797` |
| vehicle_type | text | **车型大类/型号**，如 `45HQ`、`16米5厢`、`13米7冷柜`（从 Excel 的 `45HQ-1` 拆出「型号」部分） |
| type_seq | int | **同车型内固定序号**，如 `45HQ-1` 的 `1`（0797 永远排第一） |
| type_category | text | **车型大类**（`高柜` / `厢式` / `冷柜` …），用于筛选；先试，可空 |
| operator | text | **运营公司 + 所属人名**，如 `马上供应链-韦淑琳`。**车辆固定属性，只读展示，只能后台改**（车可能从 A 公司转到 B 公司，所以必须可后台调，但不是客服在派车时改） |
| is_active | bool | 是否在册（在册总数统计用） |

> 注意：`车挂` / `柜号` **本版不做**（将来由「总表派单记录」承载）。第一版**只管车头（有车牌的整车）**，光头挂车不纳入。

#### `customers`（客户主数据，新增 —— CRM 的地基）

> 渐进式策略：第一版客户是**自由文本填写 + 历史联想**，但**后台自动把客户名沉淀成本表**。每出现一个新客户名就自动落一条记录。这样数据从第一天就是结构化的，将来做 CRM 直接在这张表上加字段（联系人、电话、对账…），**不用回头清洗、不用迁移**。

| 字段 | 类型 | 说明 |
|---|---|---|
| id | uuid PK | |
| name | text unique | 客户/货主名（如 `富`、`战`、`鲲`） |
| created_at | timestamptz | 首次出现时间 |
| created_by | uuid | 首次录入的客服 |

#### `dispatch_records`（派车记录，新增 —— 本模块核心表）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | uuid PK | |
| vehicle_id | uuid FK→vehicles | 哪辆车 |
| dispatch_date | date | 哪一天（真实日期，非「第几列」） |
| customer_id | uuid FK→customers | 哪个客户（自由填，但落库时 upsert 到 customers） |
| agent_id | uuid FK→users | **经手客服 = 录入人**。见下方归属规则 |
| is_substitute_driver | bool | **代驾标记**：这条是否靠代驾顶的（不记具体是谁） |
| is_deleted | bool | **软删**标记 |
| created_at / updated_at | timestamptz | |

**客服归属规则**：记录的 `agent_id` 永远是**录入人 a**。即使后来被管理员 b 改/删，`agent_id` 仍是 a，统计「谁经手」以 a 为准（b 的操作记在操作日志里）。

#### `dispatch_operation_logs`（操作日志，新增）

> 复用你 expense 那套留痕思路。记录谁在何时对哪条派车记录做了什么。

| 字段 | 类型 | 说明 |
|---|---|---|
| id | uuid PK | |
| record_id | uuid FK→dispatch_records | |
| action | text | `create` / `update` / `delete` |
| operator_id | uuid FK→users | 操作人 |
| detail | jsonb | 变更前后快照（可选） |
| created_at | timestamptz | |

---

## 4. 权限（What）

两种角色：

- **客服**：录入派车记录；**只能改/删自己录入的**记录。
- **管理员**：后台维护车辆主数据（含 operator 调整）；**能删别人的**记录。

通用规则：
- **删除一律软删**（`is_deleted=true`），并写 `dispatch_operation_logs`。
- **任何录入 / 删除都要二次确认**（前端弹确认）。
- Supabase **RLS** 落实上述权限（按 `agent_id = auth.uid()` 控制客服改自己的；管理员角色放开）。

---

## 5. 看板交互（How）

### 5.1 总体形态

- **Web / PC 端**，整月铺开的大看板（行=车，列=该月每一天）。
- **按月切换**（选 `2025-06` → 显示该月 1–30）。
- 行=车辆（复用主数据），格子=「车×天」。

### 5.2 单元格

- 一个「车×天」格子可能有 0 / 1 / 2+ 条记录。
- **0 条**：空格（且参与「闲置高亮」，见 5.4）。
- **1 条**：显示 `客户`（如 `富`）。
- **2+ 条**：显示 `客户 +N`（如 `富 +1`），**点开看详情**（弹层列出全部，每条含客户、客服、代驾标记）。

### 5.3 录入 / 改 / 删

- **录入**：点空格 → 弹小框 → 填**客户**（自由文本 + 历史联想下拉，选已填过的可一键带入）→ **自动带出当前登录客服** → 可勾「代驾」→ 保存（二次确认）。
- **改 / 删**：点已有标签 → 详情弹层 → 按权限显示改/删按钮 → 操作需二次确认 → 删走软删 + 写日志。

### 5.4 闲置浮现（核心功能）

两条机制都要：

1. **看板高亮**：某辆车在所选月份内**整列为空**、或**最近 N 天无派车**，则该行**高亮**（颜色标注）。`N` 做成可配置（默认建议 7 天）。
2. **均衡度排序开关**：默认按 `vehicle_type` + `type_seq` **固定序号**排（0797 永远第一，稳定好找）；提供一个「**按闲置排序**」开关，一键切换为**按本月派车次数从少到多**排（用得最少 / 最久没用的排最前）。

### 5.5 车型筛选

- 看板顶部提供**按车型筛选 / 分组**（如选「冷柜」只看冷柜车，空闲的冷柜车一眼可见）。
- 用 `type_category`（大类）做主筛选；可保留按具体 `vehicle_type` 精筛。

### 5.6 统计（看板顶部 + 行尾，全部要做）

- **行尾每辆车**：本月被派**次数 / 天数**；**距上次被派 X 天**（Excel 没有，对找闲置最直接）。
- **顶部汇总**：在册车辆总数；本月**从没被派过的车数（闲置数）**；本月派车总次数。
- **各时段用车时间分布**那行：**保留**（即便当前多为 0）。

---

## 6. 历史数据导入（How）

- 写一个**导入脚本**，把现有 Excel（3 月 / 4 月 / 5 月 / 6 月）的排车记录解析后灌入 **staging 数据库**先验证，**不直接进生产**。
- 解析规则：
  - 行 → vehicle（按 plate_no 匹配现有主数据；匹配不到的记到一个待人工核对清单）。
  - 列号 → 拼上该 sheet 月份 → `dispatch_date`。
  - 格子 `客户-客服` → 拆分；客户 upsert 到 `customers`；客服尽量映射到现有 users（映射不到的同样进待核对清单）。
  - 一格逗号分隔的多条（如 `鲲-秋,战-陈`）→ 拆成多条 `dispatch_records`。
  - 脏数据（如车牌串进客户列、`e-黄` 这类）→ 不硬塞，进**待核对清单**人工处理。

---

## 7. 技术栈与集成

- **React / Vite / TypeScript + Supabase**，与主系统**同库、同一套登录**。
- 客服身份从**已登录用户**取（`auth.uid()`），不手选。
- 权限用 **Supabase RLS** + 角色（客服 / 管理员）。
- UI 风格沿用主系统现有组件库（Tailwind / shadcn 等，按现状）。

---

## 8. Out of scope（第一版不做，但要留好接口）

| 项 | 现状决策 | 留的接口 |
|---|---|---|
| 订单 / 运单系统 | 不连单子，纯「车×天×客户」录入 | dispatch_records 将来可加 `order_id` |
| 车队长派人 / 司机信息 | 不进本表 | 代驾仅一个布尔标记 |
| 代驾具体是谁 | 只标记是否代驾 | 将来可扩 `substitute_driver_id` 指向司机/代驾主数据 |
| 车挂 / 柜号 | 不做 | 将来「总表派单记录」承载 |
| 光头挂车 | 不管，只管车头 | vehicles 可加 `is_trailer` 区分 |
| 小程序（Taro）端 | 不做 | 后续「派任务」功能可加移动端 |
| CRM 完整功能 | 仅自动沉淀 customers 表 | 在 customers 上加联系人/电话/对账等字段即长出 CRM |
| 客户写法清洗 | 自由文本 + 历史联想，暂不强约束 | customers 表已结构化，后续可做合并/去重 |

---

## 9. 给 Codex 的实现顺序建议

1. 建表 + RLS：`customers`、`dispatch_records`、`dispatch_operation_logs`；按需给 `vehicles` 补 `vehicle_type / type_seq / type_category / operator` 字段。
2. 车辆主数据只读接口 + 车型筛选。
3. 月度看板渲染（车×天网格 + 按月切换 + 单元格 0/1/N 显示）。
4. 录入弹框（客户自由文本 + 历史联想 + 自动带客服 + 代驾勾选 + 二次确认 + upsert customers）。
5. 改/删（权限判定 + 软删 + 操作日志 + 二次确认）。
6. 闲置机制（高亮 N 天可配 + 按闲置排序开关）。
7. 统计（行尾次数/天数/距上次 X 天 + 顶部汇总 + 时段分布行）。
8. 历史导入脚本 → staging，附待核对清单。
